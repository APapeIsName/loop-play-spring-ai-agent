# Round 1 — 설계 결정 문서

> 이 문서는 Round 1의 설계 결정(왜 그렇게 했는가)을 다룬다.

---

## 1단계 — System Prompt `[금지]` 섹션: 왜 이 규칙들인가

`BaedalPrompt.SYSTEM_PROMPT`(B안) `[금지]`에 다음 3가지를 두었다.

1. **타사 배달 앱을 추천하지 않습니다.**
   사용자는 감정이 격해지면 타사 배달 앱과 비교하는 발화를 빈번히 한다. AI가 이를
   그대로 받아들여 응대하면 **에이전트 스스로가 자사 서비스 신뢰를 깎는 역할**을
   하게 되어 신규 유입과 기존 유지를 동시에 잃는다.

2. **라이더/사장님에 대한 개인정보(연락처, 실명)를 노출하지 않습니다.**
   고객이 답답한 감정에 개인정보를 요구하는 상황이 실제로 발생한다. 이때 AI가
   무차별적으로 노출하면 **심각한 개인정보 유출 사고**로 직결된다.

3. **할인 쿠폰을 임의로 약속하지 않습니다.**
   할인 쿠폰은 중요한 비즈니스 의사결정의 산물이다. AI가 임의로 발급·약속하면
   곧바로 **비즈니스 손실**로 이어진다.

**왜 이 3가지인가 / 빼도 되는 것은 없는가**
세 규칙은 표면 주제(경쟁사·개인정보·쿠폰)가 다르지만 **모두 "AI의 무분별한 응대가
비즈니스 손실로 이어지는가"라는 하나의 기준**으로 수렴한다. 신뢰 손실(①),
법적·개인정보 사고(②), 직접 재정 손실(③) — 어느 하나도 손실 위험이 작지 않으므로
**제거 대상은 없다.** 추가 여부도 같은 기준으로 판단했다.

**추가 규칙 (③의 상위 일반화)**

4. **환불·보상(쿠폰 제공 등)·재배달을 사람의 확인 없이 임의로 확정·약속하지 않습니다.**

   근거: 2026년 들어 **AI로 음식 사진을 조작(파리 합성, 덜 익은 것처럼 변조 등)해
   환불·보상을 요구하는 사기**가 배달업계에서 급증, 자영업자·플랫폼이 대응에
   어려움을 겪고 있다. 상담 에이전트가 *고객 제출 주장·사진만 보고* 환불/보상/
   재배달을 임의 확정하면 **사기를 그대로 통과시켜 직접 재정 손실**을 낸다.
   기존 ③(쿠폰)이 다루던 위험을 환불·보상·재배달 전반으로 일반화한 것이며,
   "판단은 LLM, 확정·실행은 사람/검증된 시스템"이라는 본 과정 원칙과도 일치한다.

   참고 사례:
   - 식당·배달앱 'AI 조작' 사진 환불 요구 — https://v.daum.net/v/20260105200151766
   - AI 조작 사진 환불 사기 (YTN) — https://m.ytn.co.kr/news_view.php?key=202601061422591003&s_mcd=0104
   - 자영업자 'AI 조작' 영상 환불 요구 (헤럴드경제) — https://biz.heraldcorp.com/article/10649299

---

## 1단계 — `SupportResponse` 추가 필드 선택 근거

기준: "응답값 = 컨텍스트 state, 다음 인자로 쓰일 수 있는가" / 프롬프트 = 불변 정책.

**필드별 선택 근거**

- **`replyToCustomer`**: 기존 `/api/v1/support`는 트리아지 응답이라 고객에게 그대로
  보낼 답변문이 없었다. 답변문을 명시적으로 두면, **이전의 AI 평가가 잘못됐더라도
  그 답변을 고쳐 바로잡을 수 있다.**
- **`answerBasis`**: 이번 답변이 *추정*에 의한 것인지 *시스템 확인*에 의한 것인지를
  표시해, **AI가 잘못된 방향으로 갔다면 다음에 수정**하기 위함.
- **`basisReference`**: 이번 답변이 시스템 확인에 의해 말한 것이라면, **그 근거가
  무엇이었는지 확인**할 수 있다.
- **`needsHumanHandoff`**: 이 건을 다음에 **HIL(Human-in-the-loop) 루프로 넘길지**,
  아니면 AI가 스스로 판단해 처리할지를 고른다.
- **`handoffReason`**: 넘긴다면 **왜 넘기는지**에 대한 내용을 담는다.
- **`agentNextStep` / `customerAction`**: 모호하던 `nextAction`을 분해해,
  **시스템이 다음에 해야 할 일**(`agentNextStep`)과 **고객이 다음에 해줘야 하는
  행동**(`customerAction`)을 각각 명시한다.
- **`neededInfo`**: 처리에 **더 필요한 정보**를 남기기 위해 기존 필드를 그대로 유지.

요약: `replyToCustomer`·`answerBasis`·`basisReference`는 *AI의 잘못된 판단을
사후에 발견·교정*하는 데, `needsHumanHandoff`·`handoffReason`는 *HIL 라우팅*에,
`agentNextStep`/`customerAction`은 *다음 행동 주체 분리*에 기여한다.

---

## 1단계 — `Category` enum 5개 적정성 (왜 5개 / 빠진 카테고리)

데이터: S3 "라이더가 음식 엎음(보상 문의)"이 매 호출 `DELIVERY`로 분류됨
→ "보상" 전용 카테고리 부재가 드러남.

**왜 이 5개인가**
배달 서비스에 실제로 들어오는 문의를 조사해 보면, `ORDER`(주문)·`DELIVERY`(배달)·
`REFUND`(환불)·`PAYMENT`(결제) **4개가 주된 문의 유형**에 해당한다. 이 4개로 핵심
도메인을 포괄하고, 그 외 빈도가 낮거나 사소한 문의는 개별 카테고리로 세분하지 않고
`ETC`로 수용했다. 즉 "주요 4 + 포괄 1(ETC)" 구조로, 분류기를 단순하고 일관되게
유지하는 것을 우선한 결정이다.

**빠진 카테고리 / 추가한다면**
하나 더한다면 **"보상"**이 후보다. 실제로 시나리오 S3("라이더가 음식을 엎었다는데
보상 받을 수 있나요?")가 매 호출 `DELIVERY`로 분류되어, 보상성 문의가 제자리를
찾지 못하고 배달로 뭉개지는 것이 데이터로 확인됐다. 다만 현재는 그러한 비주류
문의를 `ETC`로 흡수하는 선에서 **5개를 유지**하기로 했다.

---

## 2단계 — temperature 0.3 선택 근거 (0.0/0.7 아닌 이유, 데이터로)

**데이터 1 — 분류 일관성**: temperature 0.0~1.0(0.1 간격, 각 5회) 전부
`categoryConsistency = 1.0`. 스키마(`@JsonPropertyDescription`)가 분류를 강하게
고정해 **분류 일관성은 temperature와 무관** → 분류 지표로는 0.3을 정당화할 수 없음.

**데이터 2 — 고객 답변문(`replyToCustomer`) 변동성** (5개 중 서로 다른 문장 수):

| temp | 서로 다른 문장 | 양상 |
|---|---|---|
| 0.0 | 1/5 | 5개 완전 동일("현재 배송 중입니다. 조금만 기다려주세요.") — 기계적 정형문 |
| 0.1 | 2/5 | 사실상 결정적 |
| 0.3 | 5/5 | 문장이 자연스럽게 다양해지는 변곡점 |
| 0.5~0.7 | 5/5 | 변동 큼 |
| 1.0 | 5/5 | 변동 큼 + 환각(시스템 조회 없이 ETA 날조 등) |

**왜 0.3인가 (0.0/0.7이 아닌 이유)**
분류는 temperature와 무관하게 안정적이므로, 판단 기준은 *고객 답변문의 질*이 된다.
- `0.0`·`0.1`: 일관성은 지키지만 **매번 거의 똑같은 정형문**이라 상담 톤이 기계적이다.
- `0.3`: 답변이 **자연스럽게 다양해지면서도 과하지 않은** 지점이다.
- `0.7` 이상: 변동이 커지며 **환각(없는 ETA 날조 등) 가능성이 높아진다**(0.3에도 1건 관찰되긴 함).

→ 일관성을 해치지 않으면서 답변이 *로봇처럼 굳지도(0.0), 환각으로 튀지도(0.7+)*
않는 절충점이 **0.3** 이므로, 상담 도메인에 가장 적합하다고 판단했다.

---

## 2단계 — 구조화 vs 단순 프롬프트 (어떤 상황에서 단순이 더 나은가)

**측정값**: T1a(단순 vs 구조화 System Prompt, "배달 어디쯤", 5회)·모호 재실험
("취소+환불", 10회) **둘 다 `categoryConsistency = 1.0`** — 분류 지표상 차이 0.

**"구조화"는 2층으로 봐야 한다.**
- (a) **System Prompt 구조화**: B안의 [역할]/[규칙]/[금지]/[응답 포맷]
- (b) **스키마/필드설명 층**: `.entity()` + `@JsonPropertyDescription`(필드별 규칙)

`@JsonPropertyDescription`은 system prompt와 무관하게 `.entity()`가 **항상 주입**한다.
즉 T1a의 "단순 프롬프트"도 (b)는 켜져 있었고, 실제로 토글된 건 (a)뿐이다. 결과가
near-parity라는 것은 곧 **"(a) System Prompt 구조화는 분류 일관성에 거의 기여하지
않고, 구조화의 실효는 (b) 스키마/필드설명 층에서 나온다"** 를 의미한다.

**교차 증거 ((b)층을 실제로 뺐다 넣은 자연 실험 = Q1 검증 #1 ↔ #2):**
- 검증 #1 ((b)층 = `@JsonPropertyDescription`/`required` 없음): `answerBasis`·`urgency`
  등이 S2·S3에서 **null**, 분류 흔들림.
- 검증 #2 ((b)층 추가): **전 필드 충전**, S2 분류 `ORDER → REFUND` 교정.
- → (b)층 유무로 출력이 극적으로 바뀜 = 구조화의 힘은 (b)에 있다는 직접 증거.

**구조화가 단순보다 나은가 / 언제 단순이 더 나은가**
"구조화가 낫다"는 **참**이되, 그 효력은 주로 (b) 스키마/필드설명 층에서 나오며
(a) System Prompt 문장 구조화는 그 위에 한계적으로만 더한다. 따라서 (b)층이 이미
출력을 강하게 강제하는 상황에서는, (a)를 길게 구조화해도 분류 일관성 이득은 미미한
반면 **입력 토큰만 증가**한다(System Prompt 2배 → 입력 토큰 1.19x 실험과 연결).
그런 경우에는 **System Prompt를 단순하게 유지하는 편이 비용 효율적**이다.

---

## 3단계 — 스트리밍 적용 범위 판단

**측정 데이터**: 동기 `/api/v1/chat` TTFB 15.94s vs 스트리밍 `/api/v1/chat/stream`
TTFB 1.42s(≈11배), SSE 토큰 160청크 (`round1-quest3-streaming.md`).

**핵심 판단 — 실시간성 ↔ 출력의 구조적 사용가능성 트레이드오프**
스트리밍은 응답을 *REST의 JSON 객체처럼 구조화된 형태*에서 *구조화되지 않은
토큰 흐름*으로 바꾼다. 따라서 `/api/v1/support`·triage처럼 **구조가 중요한 곳에는
투입할 수 없다**: `.entity()`는 완성된 JSON을 받아야 역직렬화하는데, 스트리밍은
반쪽 JSON 조각만 흘려 **역직렬화 실패 → 다운스트림 라우팅·검증 불가 →
시스템 신뢰성 저하**로 이어진다.

즉 이것은 **실시간성(체감 속도)과 안정성(= 출력의 구조적 사용가능성·무결성)
사이의 트레이드오프**이며, 어느 쪽을 택할지는 엔드포인트 성격에 따른 **비즈니스적
선택**이다. → "모든 엔드포인트 적용"이 아니라 *대화형엔 스트리밍, 트리아지·구조화엔
동기*의 **선택적 적용**.

**부분 실패 측면(트레이드오프 보강 근거)**: 스트리밍은 응답 시작 후엔 깔끔한 HTTP
에러코드를 반환할 수 없어(이미 일부 전송됨) 에러·재시도 UX가 어렵다. 반면
동기/구조화는 실패 시 명확한 4xx/5xx로 안정적으로 처리·재시도된다.

**하이브리드도 고려할 수 있다**: 트레이드오프를 반드시 택1로 풀 필요는 없다.
한 LLM 호출에서 *사용자에게는 토큰을 스트리밍해 체감 속도를 주고, 시스템에는
구조화 결과를 확정해 판단·실행에 쓰는* 병행 구조도 고려 가능하다 — "LLM은
판단자, 실행은 서버" 원칙과 부합한다.

**프론트엔드 변화**: 전면 교체가 아니라 *부분·추가*다. 구조화 엔드포인트는
그대로 REST(동기) 유지하고, **대화형만** `EventSource`(SSE — 서버→클라 단방향
이라 WebSocket은 과함) 등으로 **점진 수신·렌더 + 스트림 중간 에러/완료 처리**를
추가한다. 결국 프론트는 *동기 구조화 응답*과 *스트리밍 비구조화 응답*
**두 가지를 모두 수용**할 수 있게 준비해야 한다.
