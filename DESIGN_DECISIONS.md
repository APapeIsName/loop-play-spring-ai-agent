# Round 2 — 설계 결정 문서

QUEST 1·2·3단계가 요구한 설계 결정 문항에 대한 답.

## 1단계

---

## (1) `OrderDetailView`는 `Order`의 어떤 필드를 의도적으로 뺐는가? 그 이유는?

### 무엇을 뺐나

`Order`의 11개 필드 중 4개를 `OrderDetailView`에 의도적으로 제외했다.

| 필드 | 포함 여부 | 이유 |
|---|---|---|
| `deliveryAddress` | ❌ 제외 | 배송지 주소는 개인정보. 주문 *내역* 조회에 필요 없고, 노출되면 사고. |
| `riderLocation` | ❌ 제외 | "라이더 위치"는 `getDeliveryStatus` 전담 영역. 두 Tool의 책임을 깨끗하게 분리하기 위해 detail에는 의도적으로 안 넣음. |
| `canceledReason` | ❌ 제외 | 운영·CS 내부 자료. 고객은 자기가 말한 취소 사유를 모를 리 없고, 다른 고객 사유가 섞여 노출될 가능성을 차단. |
| `canceledAt` | ❌ 제외 | `status == CANCELED`라는 사실만으로 고객 응대에 충분. 정확한 취소 시각은 내부 감사용. |

### 왜 이렇게 자른 기준

세 가지 기준이 동시에 작용했다.

1. **개인정보·민감정보 회피.** `deliveryAddress`처럼 그 자체로 식별 가능한 정보는 LLM이 다른 응답 맥락에서 흘릴 위험을 원천 차단.
2. **Tool 간 책임 분리.** `riderLocation`은 `getDeliveryStatus`만 다룬다는 약속을 DTO 수준에서 강제. 두 Tool description의 비대칭(detail=좁게, delivery=모호한 상태 질문의 기본값)을 데이터로도 뒷받침.
3. **토큰 경제성.** Tool 응답은 매번 2차 LLM 호출 입력 프롬프트에 통째 직렬화돼 들어간다. 불필요한 필드는 매 호출마다 입력 토큰 비용을 누적. 4개 필드 절약은 한 호출당 작아 보여도 운영 단위에선 누적 효과 큼.

### Round 1과 같은 원칙, 다른 적용

Round 1 `SupportResponse`에서도 "응답값 = 컨텍스트 state, '다음 인자로 쓰일 수 있는가' 통과한 것만 필드화" 기준을 썼다. Round 2의 DTO 절단도 같은 원칙의 다른 적용 — *"이 필드가 LLM의 다음 응답에 쓰일 정당한 이유가 있는가? 없으면 빼라."*

---

## (2) `@Tool` description을 한국어로 썼는가, 영어로 썼는가? 어떤 기준으로?

**한국어로 작성.**

### 결정 기준 — 발화 언어와 매칭 거리

이 에이전트는 한국어 발화를 받는다. LLM이 "사용자 발화 → 어떤 Tool인가" 판단할 때 발화와 description의 의미·어휘적 거리가 짧을수록 매칭이 정확해진다. 발화 \"어떤 메뉴 주문했어요?\"와 description \"메뉴, 수량, 결제 금액 같은 구체적인 주문 내역\"이 같은 한국어로 적혀 있으면 *명사·동사 매칭이 직접* 일어남. 영어 \"Get order details (menu, quantity, payment)\"였다면 모델이 한 번 더 cross-lingual 매칭을 거쳐야 한다.

강의 자료 2.4 권고도 이 방향: *"description은 한국어로 작성하세요. LLM이 한국어 사용자의 발화와 매칭하는 거리가 짧아져 호출 정확도가 올라갑니다."*

### 실험으로도 입증된 측면

- v1 50회 측정에서 scn 1(\"어디쯤?\")이 한국어 description으로 10/10 호출. \"어디쯤\"이라는 한국어 어휘가 description의 \"어디쯤\" 예시와 직접 매칭.
- v2~v5에서 System Prompt를 만지작거리며 합계가 84%→38%까지 떨어지는 동안에도 scn 1만은 상대적으로 안정적이었던 이유 중 하나가 한국어 발화-한국어 description 매칭의 직접성.

### 단점도 있다

- 영어권 코드리뷰어가 description을 이해 못 할 수 있음. → 코드 주석은 영어로, description은 한국어로 분리하는 게 깔끔.
- 다국어 발화가 들어오는 시점에는 multilingual description이 필요. 그때 가서 고민하면 됨 — YAGNI.

---

## (3) `OrderTools`를 하나의 클래스로 묶었다. 분리 기준은? 현재 수준에서 하나로 충분한 이유는?

### 분리한다면 무엇을 기준으로

가능한 분리 축은 셋이다.

1. **읽기 vs 쓰기 (CQRS-ish).** `getOrderDetail`/`getDeliveryStatus` (조회) vs `cancelOrder` (변경). 변경 연산은 트랜잭션·멱등성·감사 로깅이 추가로 필요하므로 향후 분리 가치 있음.
2. **도메인 단위.** `OrderTools`(주문) / `DeliveryTools`(배달·라이더) / `PaymentTools`(결제·환불). 도메인이 커지면 자연스럽게 갈라짐.
3. **호출 빈도·민감도 단위.** 자주 호출되는 안전한 조회 Tool과 드물게 호출되는 위험한 변경 Tool을 분리해 권한·감사·rate limit 적용을 다르게.

### 지금 하나로 충분한 이유

현재 3개 메서드 모두 `Order` 도메인 한 묶음을 다룬다. 분리하면 클래스 1개 → 3개로 늘면서 매번 import·DI 배선·테스트 setup만 늘어남. **분리 비용 > 분리 효용**.

또 강의의 한 줄 메시지 — *"Tool을 만드는 것보다 Tool의 경계를 설계하는 훈련이 핵심"* — 도 같은 결: 경계는 *Tool 단위 description으로* 이미 그어져 있다(detail vs delivery vs cancel). 그 위에 *클래스 단위 분리* 까지 얹는 건 같은 경계를 두 번 표현하는 중복.

### 어느 시점에 분리할 것인가

- **`cancelOrder`가 외부 PG 호출을 포함하는 순간** — 그 호출은 별 트랜잭션·실패 재시도·outbox 패턴이 필요. 이 시점에 변경 Tool은 `CommandOrderTools`로 빼서 인프라 의존(PG 클라이언트, 메시지 발행)을 그쪽에 격리.
- **결제·포인트·환불 같은 *별 도메인* Tool이 추가되는 순간** — `OrderTools`에 더 넣으면 클래스가 책임 다발이 됨. 도메인 단위로 갈라.
- **민감도가 다른 Tool이 섞이는 순간** — 예: 운영자만 호출 가능한 Tool은 `AdminOrderTools`로 분리해 권한 검사를 클래스 단위로 일괄 적용.

지금은 셋 다 해당 안 됨. 하나로 둔다.

---

## 2단계

## (4) `Outcome` enum이 왜 4개인가? `UNKNOWN`/`FAILED`는 왜 넣지 않았는가?

### 결론

**LLM에게 이유를 명확히 알려주기 위함** + **그 값 자체가 응답으로 쓰기 좋다고 판단**해 4분기로 갈랐다.

### `UNKNOWN`/`FAILED`를 배제한 이유

만약 `UNKNOWN`이나 `FAILED`가 enum에 있다면, LLM 입장에서 그건 마치 **HTTP 500 같은 *서버 내부 장애*** 처럼 받아들여진다. 시스템이 이유를 짚어주지 못한 채 "실패"라는 결과값만 전달하는 셈이고, 그러면 LLM은 *왜* 실패했는지를 직접 추론해야 한다 — 그 추론 과정에서 잘못된 응답값이 내려갈 가능성이 높다.

(우리는 1단계 v1 scn 4의 reason hallucination 패턴, v5의 JSON-as-text 출력 등에서 *LLM이 정보 부족 시 어떻게 잘못 추론하는지* 이미 충분히 봤다. 시스템이 명확한 outcome을 안 주면 LLM은 더 자주 그런 모드로 빠진다.)

### 4분기가 *확실한 답변 유도*에 적절한 단위

각 outcome이 LLM에 *명확한 응답 톤*을 주문한다 — 실제 quest2-outcome 측정에서 LLM이 만든 응답:

- `CANCELED` → *"주문이 정상적으로 취소되었습니다. 결제 취소는 카드사에 따라 최대 7영업일이 소요될 수 있으니 참고해주세요."*
- `ALREADY_CANCELED` → *"주문이 이미 취소된 상태였습니다."* (baseline pair 4)
- `NOT_CANCELABLE` → *"죄송합니다, 현재 주문은 조리 과정에서 완료된 상태라서 취소가 불가능합니다. 이 경우에는 상담원과 직접 연결하여 문제를 해결해 보는 것이 좋을 것 같습니다."*
- `NOT_FOUND` → *"죄송합니다, 해당 주문번호로 정보를 찾을 수 없습니다. 다시 한번 확인해 주실 수 있을까요?"*

4개 다 서로 다른 톤·다른 후속 행동(상담원 안내 / 입력 재확인 / 결제 취소 안내 등)을 가진 응답. 만약 `FAILED` 하나로 묶었다면 *"취소에 실패했습니다"* 정도에 그쳤을 것이고, **비즈니스적으로 애매모호한 답변으로 고객 기분을 나쁘게 만드는 UX**가 됐을 것이다.

### 핵심 원칙

**AI를 사용함에 있어 *확실한 답변 유도*가 시스템의 책임**. enum 이름·분기 갯수가 LLM 응답 품질을 직접 결정한다. 강의 자료 §3.3 *"이 enum은 LLM이 읽는다. 자연어처럼 읽혀야 의미를 파악한다"* 와 동일선상.

## (5) 배달 운영에서 실제 추가될 법한 Outcome 2개

### 결론 — `REJECTED_BY_STORE` + `REQUIRES_AGENT`

코드베이스 + 실제 배달 운영(배민·쿠팡이츠·요기요) 리서치를 종합해, 운영 빈도가 높고 우리 Tool의 경계를 명확히 그어주는 두 가지를 골랐다.

### Outcome A — `REJECTED_BY_STORE`

**시나리오**: 사장님(매장)이 주문을 거절. 대표 사유:

- 배달 가능 범위를 벗어난 거리
- 주문 폭주 / 배달 인력 부족
- 재료 부족
- 영업 시간 외
- 음식점 내부 사정

이건 *고객이 `cancelOrder`를 부르기 전에 이미 매장이 거절한 상태*. 우리 `OrderStatus`는 사장님 거절을 별도로 모델링하지 않고 `CANCELED`로 흡수하지만, **Tool 응답 측면에서는 "이건 매장 측 거절로 종료된 주문이다"를 LLM이 알아야** 자연어 응답이 정확해진다.

**시스템 분기**: Tool은 새 상태 변경을 하지 않고, 매장 거절 사실 + 자동 환불 안내만 반환.

**LLM 응답 톤**: *"매장 사정으로 주문이 거절되었습니다. 결제는 자동 환불 처리되며, 다른 매장을 추천해드릴 수도 있습니다."*

**Tool 경계 설계 의미**: cancelOrder Tool이 *읽기 측에서 만나는 외부 상태* (Tool이 만든 게 아니라 *발견*한 결과). 우리 4개 outcome이 *Tool의 능동 분기*였다면, 이건 *환경에서 읽어들이는 수동 분기*.

### Outcome B — `REQUIRES_AGENT`

**시나리오**: 자동 처리가 부적합한 케이스 — 사람(상담원) 개입 필수.

- 이물질·부패 등 보상 분쟁 케이스
- 라이더 사고·배달 사고로 보상 책임 모호한 상황
- VIP 고객 / 분쟁 이력 있는 고객
- 환불 정책 결정이 필요한 비표준 요청 (예: 부분 환불·쿠폰 보상 협의)

**시스템 분기**: 상담원 큐로 티켓 전달 + 컨텍스트(주문 정보·고객 발화 reason)와 함께 dispatch. 상태는 그대로 두고 \"검토 대기\" 라벨만 추가.

**LLM 응답 톤**: *"상황을 확인하시려면 상담원 연결이 필요합니다. 곧 연결해드리겠습니다. 잠시만 기다려주세요."*

**Tool 경계 설계 의미**: Tool이 *쓰기 책임을 사람에게 위임*하는 결정. 강의 자료의 "판단은 LLM, 실행은 Spring Bean" 원칙의 *한 단계 위* — *"판단·실행 모두 사람"* 으로 경계가 한 번 더 그어진다. AI 시대에도 *사람이 책임지는 마지막 보루*가 분명히 있어야 한다는 설계 신호.

### 리서치 배경 — 실제 배달 운영의 빈도 높은 패턴

배달앱 3사(배민·쿠팡이츠·요기요)의 공개된 정책·CS 가이드·뉴스 리뷰를 종합해 본 실제 운영 패턴:

1. **사장님 거절** (`REJECTED_BY_STORE`): 거리·재료·시간·인력 등 매장 측 사정. **빈도 매우 높음** — 가게 노출 거리·배달 인력 매칭 한계가 늘 있는 구조라 일상적 발생.
2. **플랫폼 강제 자동 취소** (`AUTO_CANCELED_BY_PLATFORM`): 배민은 객관적 사유 (이물질·누락·훼손·합의 안 된 지연 등) 확인 시 외식업주 동의 없이 직접 취소·환불 처리. 환불액은 외식업주 부담.
3. **부분 환불·쿠폰 보상** (`PARTIAL_REFUND_AVAILABLE`): 쿠팡이츠는 배달 지연 20분 이상 시 부분 환불 또는 쿠폰 보상 제공.
4. **분쟁성 케이스**: 오배달·이물질·라이더 사고 등은 사람 개입 필수.
5. **취소 가능 시점 분기**: 세 앱 모두 *접수 전 → 직접 취소*, *접수 후·조리 시작 전 → 매장 동의 필요*, *조리 중·배달 중 → 앱 내 불가, 고객센터로* 구조 (우리 `NOT_CANCELABLE`이 이 마지막 단계와 매핑).

### 다른 후보들 — 보류 이유 요약

| 후보 | 시나리오 | 빈도 | 보류 이유 |
|---|---|---|---|
| **`AUTO_CANCELED_BY_PLATFORM`** | 플랫폼이 품질·사고 이슈로 강제 취소 | ⭐⭐ | 매력적이지만 우리 도메인엔 *플랫폼 측 정책 엔진*이 모델링 안 됨. 도입 시 별 시스템(이물질 신고 큐 등)이 함께 필요. |
| **`PARTIAL_REFUND_AVAILABLE`** | 배달 지연 등으로 부분 환불·쿠폰 보상이 더 적절 | ⭐⭐ | 결제·쿠폰·포인트 도메인이 우리 모델에 없음. cancelOrder 단일 Tool로 처리하기 부적합 — `RefundTools` 같은 별 도메인 Tool이 필요. |
| **`COOLING_OFF`** | abuse 방지용 짧은 잠금 (반복 취소 시도) | ⭐ | 시스템 안전판 성격으로 운영 빈도 낮음. 실제로 우리 Stage B 데이터에서 본 *반복 cancel 호출 패턴*은 멱등성(`ALREADY_CANCELED`)으로 이미 처리됨. |

### 두 outcome 추가 시 enum 모양

```java
public enum Outcome {
    CANCELED,
    ALREADY_CANCELED,
    NOT_CANCELABLE,
    NOT_FOUND,
    REJECTED_BY_STORE,    // ← 신규: 사장님 측 거절로 이미 종료된 주문
    REQUIRES_AGENT        // ← 신규: 자동 처리 부적합, 상담원 위임
}
```

여전히 Q4의 두 기준 — *시스템 분기 + LLM 자연어 매핑* — 을 둘 다 만족. 6개로 늘어나도 *애매한 묶음 outcome* 없이 각 분기의 의미가 깨끗하다.

## (6) 멱등성 3수준 중 "같은 응답 재전달" 택한 이유 + 다른 상황에서 "에러"가 적절한 예

### (a) "같은 응답 재전달" 택한 이유

에러를 주거나 무시하게 되면 LLM이 *다시 추론하거나* 응답값을 *서버 오류로 잘못 내려줄 수* 있다. 반면 기존값 재전달은 **사실 그대로** 재전달하는 것일 뿐이고, LLM은 그 사실을 받아 적절한 자연어 응답을 만들면 된다.

이건 Q4의 "확실한 답변 유도" 원칙과 같은 결 — *LLM에 정보를 주지 않으면 추론하고, 추론은 hallucinate를 부른다*. 우리 2단계 baseline pair 4에서 본 실제 응답으로 입증:

```
1st: [Tool] cancelOrder(orderId=2024-1239, reason=집 앞에 사람이 없어요)
     → "주문이 정상적으로 취소되었습니다. 결제 취소는 카드사에 따라 최대 7영업일이 소요될 수 있습니다."
2nd: [Tool] cancelOrder(orderId=2024-1239, reason=한 번 더 취소해주세요)
     → "주문이 이미 취소된 상태였습니다. 다시 한번 확인하겠습니다."
```

`ALREADY_CANCELED` outcome이 LLM에 *과거 사실*을 그대로 전달했고, LLM은 그 사실을 자연어로 정확히 표현했다. 만약 *에러*였다면 "취소에 실패했습니다"로 잘못 전달했을 것이고, *무시*였다면 응답 톤 자체가 모호해졌을 것.

### (b) "에러"가 더 적절한 예 — 결제·재고 차감 이중 호출

cancelOrder는 *상태 전이가 한 방향* (CANCELED → 다른 상태로 못 돌림) 이라 두 번 호출해도 결과가 같다 → 멱등 success로 묶기 안전하다.

반면 *두 번째 호출이 다른 자원에 영향을 주는* 액션은 다르다.

**예: 구매 결제 / 재고 차감**

클라이언트가 *원래는 1번 발생해야 하는* 액션(결제 API 호출, 재고 1 차감)을 *어쩌다 2번* 보내는 상황 — 네트워크 재시도, 사용자 더블 클릭 등. 시스템이 멱등성을 강제하기 위해 두 번째 시도를 *명시적 에러*로 거절하면, 클라이언트가 **"중복 요청이었다"** 를 인지할 수 있다.

- *멱등 success로 묶었을 경우*: 클라이언트가 "두 번 다 성공"으로 인지 → 정작 결제는 1번만 됐는데 클라이언트는 *두 번 받았다고 안심* → 정산·재고·매출 데이터 불일치 위험.
- *에러로 거절*: 클라이언트가 명시적으로 "중복"을 알게 되어 재시도 의사결정 / 사용자 알림 / 로그 분석 등 후속 액션 가능.

### cancelOrder vs 결제 — 두 패턴의 결정적 차이

**두 번째 호출이 클라이언트(또는 사용자)의 *의사결정을 바꾸는가?***

- **cancelOrder**: 안 바꿈. 이미 취소된 상태에서 한 번 더 눌러도 사용자에게 추가로 알려줄 *새 정보*가 없다(이미 취소됨). → 멱등 success가 자연스럽다.
- **결제 / 재고 차감**: 바꿈. 이중 처리 위험을 클라이언트가 알아야 *진짜로 결제가 됐는지·재시도해야 하는지* 결정할 수 있다. → 에러로 명시 거절이 안전하다.

즉 "같은 응답 재전달"이 항상 정답은 아니고, **두 번째 호출이 무엇을 결정하게 하느냐**에 따라 세 수준을 골라야 한다.

---

## 3단계

## (9) description 작성 시 반드시 포함해야 할 4가지 — 실험으로 체감한 중요도 순

### 결론 — **무엇 > 입력 > 언제 > 실패**

강의 자료의 4가지를 *우리 1·3단계 실험으로 체감한 중요도*로 재배열한 결과. 판단 기준은 한 줄로 — *LLM이 자체로 못 채우는 정보일수록 우선*. 그리고 *Tool의 계약(contract)이 호출 부수 정보보다 본질적*.

### 각 요소의 역할

- **무엇**: LLM이 description을 보고 *Tool의 정체성을 직접 판단*하는 근거. description 외엔 단서가 없어 *명시 없으면 Tool이 존재하지 않는 효과*.
- **입력**: Tool을 부를 *형태(틀)*. 형식·타입·예시. LLM이 발화에서 임의로 추론하면 잘못된 값을 만들어내는 위험 영역 — *데이터 품질 직격*.
- **언제**: 발화에서 호출 시그널을 잡는 트리거. AI가 발화 어휘 분석으로 *어느 정도 자체 추론 가능*한 영역.
- **실패**: 호출 결과가 나온 *이후 처리*. LLM이 자연어로 알아서 합리적으로 처리하는 부가적 요소.

### 두 묶음으로 보는 4요소

- **계약(contract)** = **무엇**(Tool 정체성) + **입력**(받는 값의 형태). Tool 자체를 *정의*하는 정보.
- **호출 부수 정보** = **언제**(트리거 조건) + **실패**(사후 처리). Tool이 *어떻게 쓰이는지·뭐가 되돌아오는지*에 대한 보조.

→ 계약이 부수 정보보다 본질적이라 *무엇·입력*이 *언제·실패*보다 위.

### 무엇 > 입력인 이유

깨졌을 때 *무엇 쪽이 더 깊이 무너진다*.

- **무엇이 깨지면**: Tool 자체가 *존재하지 않는 효과* — 3단계 B(빈약 한 줄) → **0/10 호출**, C(오해 유발) → **0/10 호출**. LLM이 호출 자체를 결정 못 함.
- **입력이 깨지면**: *호출은 되지만 잘못된 인자로* — 1단계 v1 scn 4에서 reason 단서 없는 발화에 9건이 동일 hallucination(`reason="집 앞에 사람이 없어요"`). 호출은 일어났지만 데이터 품질 사고.

존재 자체 사라짐 > 데이터 품질 사고 → **무엇 > 입력**.

### 입력 > 언제인 이유 (핵심)

판단 기준은 *"LLM이 자체로 채울 수 있는가"*. 이 기준으로:

- **입력은 LLM이 자체로 못 채움** — `orderId` 형식(`YYYY-XXXX`)이나 `reason`의 의미는 description 없이는 발화에서 *임의 추론*되며, 그 결과 hallucinated 값이 박힌다 (1단계 scn 4의 9/9 동일 reason). description의 *틀 정의*가 없으면 *데이터 품질 직격*.
- **언제는 LLM이 어느 정도 자체 채움** — 발화 어휘(*"어디쯤?"*, *"메뉴?"*) 분석으로 *발화-Tool 매핑*을 모델이 추론 가능. A 버전(언제 누락된 강의 자료)에서도 5/10은 호출됨 → 언제 부재의 영향은 *호출 빈도 일부 감소*에 그침.

호출 빈도 감소(언제 부재) < 데이터 품질 사고(입력 부재) → **입력 > 언제**.

### 실패가 맨 뒤인 이유

LLM이 결과를 받고 *자연어로 후처리*하는 단계라 description 명시 없어도 모델이 알아서 해석한다. 1단계 scn 5(없는 주문번호 2099-9999)에서 Tool이 null을 반환했을 때 LLM이 *"죄송합니다, 해당 주문번호로 정보를 찾을 수 없습니다. 다시 한번 확인해 주실 수 있을까요?"* 같은 응답을 자연스럽게 만들어 줌 — 실패 처리 가이드 없이도.

### 데이터 종합 표

| 순위 | 요소 | 깨졌을 때 / 부재했을 때 데이터 | 영향 깊이 |
|---:|---|---|---|
| 1 | **무엇** | 3단계 B(빈약) → **0/10**, C(오해) → **0/10** | Tool 존재 자체 사라짐 |
| 2 | **입력** | 1단계 scn 4 → **9/9 가짜 reason** 동일 hallucination | 호출 됐지만 데이터 품질 사고 |
| 3 | **언제** | 3단계 A([언제] 부재) → **5/10**. 4요소 풀버전(언제 명시) → **10/10** | 호출 빈도 일부 감소 |
| 4 | **실패** | 1단계 scn 5 null → LLM이 *"찾을 수 없다"* 자연스럽게 안내 | LLM이 알아서 처리 |

→ **무엇 > 입력 > 언제 > 실패**.

## (10) description이 "오래된 주석"처럼 실제 Tool 동작과 어긋나는 상황 — 프로덕션 방지 대책

description 회귀(코드는 바뀌었는데 description은 옛날 그대로, 또는 description부터 잘못 적힌 경우)를 **4단으로 막는다**.

### 1. 사전 방지 (개발 시점) — JavaDoc / lint

코드 변경 시 description을 같이 안 고치는 *"오래된 주석"* 사고를 막는 가장 앞단. 메서드 시그니처가 바뀌면 description의 [입력] 섹션도 함께 바뀌었는지 빌드 단계에서 검증. JavaDoc과 `@Tool(description=...)`의 동기화를 lint 규칙으로 강제 가능.

### 2. AI Agent 협업 시점 — `CLAUDE.md`(또는 동등) 메타 문서

AI Agent로 코드를 생성하는 시대에는 *description 작성 규칙 자체를 AI에게 가르치는 문서*가 필요하다. 우리 프로젝트에도 `CLAUDE.md`에 *"description 작성 시 4요소(무엇/입력/언제/실패) 포함, 한국어, 4섹션 형식"* 같은 규칙을 박아두면 AI가 새 Tool을 만들 때도 *우리 컨벤션*을 자동으로 따른다.

이건 단순 문서화 이상이다 — *AI 생성 코드의 회귀를 사전에 막는* 협업 규약. *"이런 description은 우리 프로젝트에서 안 받음"* 의 라인을 AI가 사람 리뷰어 없이도 자체 검증할 수 있게 된다.

### 3. 검증 시점 — Contract Test

description 변경이 *실제 Tool 호출 거동에 어떤 영향을 주는지* 자동 검증. 우리 3단계 실험(A/B/C × 10 trial × 호출률 측정)이 정확히 이 패턴의 정량 버전.

발화 fixture 셋(예: `"주문번호 2024-1234 배달 어디쯤이에요?"`)과 기대 Tool/호출률 임계(예: `getDeliveryStatus`, 90% 이상)를 정해두고, description 변경 PR이 들어올 때마다 자동으로 N회 호출 → 호출률·outcome이 임계 이하로 떨어지면 빌드 실패. 우리 quest3-{A,B,C}.jsonl 측정 방식을 그대로 CI에 박을 수 있다.

### 4. 사후 감지 (운영 시점) — 모니터링 + 알람

CI에서 못 잡은 회귀의 마지막 안전망. *채팅 트래픽*과 *Tool 호출률*을 일별·시간별로 추적:

- description별 Tool 호출률 일평균 (예: 90% → 50% 급락 시 회귀 의심)
- 평균 입력 토큰 / 응답 시간 추이
- outcome 분포 변화 (예: `ALREADY_CANCELED` 비율 갑작스러운 증가)

급락·이상치 발생 시 슬랙·이메일 알람 → 운영 팀이 빠르게 회귀 PR을 추적·롤백.

### 4단 묶어서 보면

- *사전 방지*(1·2)에서 막지 못한 회귀를 *검증*(3)에서 잡고, 그것도 놓친 회귀를 *사후 감지*(4)에서 잡는 **다층 방어(defense in depth)**.
- 어느 한 단계만으론 부족 — *사람이 빠뜨릴 수 있는 자리*가 각 단계마다 다르다.
- 우리 v1~v5·A/B/C 실험 자체가 *Contract Test + 모니터링*의 prototype.
