# Round 1 — AI 코드 리뷰 (4단계)

## 대상 코드

AI에게 "Spring AI로 배달 상담 챗봇을 만들어줘"라고 요청해 받은 코드
(`DeliveryChatbotApplication` + `ChatController` / `ChatClientConfig` /
`DeliveryTools` / `OrderService` / dto / `application.yml` / `index.html`).

> 참고: 이 코드는 흔한 클리셰(API Key 하드코딩·시스템 프롬프트 없음·동기 호출만·
> 로깅 없음)는 **대부분 피했다.** 시스템 프롬프트(페르소나/업무범위), `ChatMemory`,
> `@Tool` Function Calling, `SimpleLoggerAdvisor`, env 기반 키, 스트리밍 엔드포인트가
> 모두 있다. 따라서 진짜 프로덕션 결함은 더 안 보이는 **보안/접근통제** 쪽에 있다.

---

## 문제점 1 — 인증 없이 주문번호만 알면 남의 주문을 취소할 수 있음

**근거**: `ChatController`에 인증 전무, `DeliveryTools.cancelOrder` →
`OrderService.cancelOrder(orderId)` 어디에도 **주문 소유자 검증이 없다**.
공격자가 `"ORD-2026-1002 취소해줘"`라고만 해도 타인의 주문이 비가역적으로
취소된다(금전·운영 사고). 주문번호는 *식별자*일 뿐 인증수단이 아니다 — 전형적
접근통제 붕괴(IDOR)이며 파괴적 액션이라 영향이 크다.

**개선 방안**: 요청자를 먼저 인증(로그인 세션/JWT/API 토큰)하고, Tool 실행 전
`주문.소유자 == 인증된 사용자`인지 **소유권을 검증**한다. 특히 `cancelOrder`처럼
되돌릴 수 없는 액션은 소유권 검증에 더해 본인 확인/2차 확인 단계를 둔다.

---

## 문제점 2 — 클라이언트가 conversationId를 그대로 지정 → 대화 탈취·이력 유출

**근거**: `ChatController.resolveConversationId`가 요청 본문의 conversationId를
그대로 `ChatMemory`의 키로 사용한다. 사용자-식별자에 묶이지 않으므로, 공격자가
타인의 conversationId를 보내면 **그 사람의 대화 맥락을 읽고 이어갈 수 있다**
(대화 탈취·개인정보 유출).

**개선 방안 (b안 채택)**: 클라이언트가 conversationId를 **자유롭게 지정하지
못하게** 한다. 인증된 `userId`를 기준으로 **서버가 conversationId를 결정·
네임스페이스**한다(예: `userId` + 클라이언트 측 스레드 식별자 조합). 이렇게 하면
사용자는 구조적으로 *자기 것 이외의 conversationId를 가리킬 수 없다*. 핵심은
"클라이언트 지정값을 신뢰하지 않고, 인증과 결합해 소유를 보장"하는 것.

---

## 문제점 3 — 크로스사이트 악용 + 비용 증폭(무제한 호출)

**근거**: `ChatController`에 `@CrossOrigin(origins = "*")` + 인증 없음 +
입력 길이 제한 없음. LLM 백엔드라 호출마다 토큰 과금이 발생하는데 레이트리밋도
없다 → 어떤 사이트·스크립트든 무제한 호출 가능하고, 초장문 입력/반복 호출로
**API 비용 폭증·DoS**가 가능하다.

**개선 방안**: "익명·무제한" 지점을 제한으로 닫는다 —
(1) CORS를 허용 도메인 **화이트리스트**로 제한(`*` 제거),
(2) **인증 필수화**(문제점 1·2의 인증과 연결되어 익명 무제한 호출 차단),
(3) 사용자/IP별 **레이트리밋**(시간당 요청 수),
(4) **입력 길이·토큰 상한** 및 per-user 쿼터.

---

## 종합

세 결함은 한 뿌리에서 나온다 — *"익명 · 클라이언트 지정값 신뢰 · 무제한"*.
이를 *"인증 · 서버의 소유 검증 · 상한"* 으로 닫는 것이 개선의 핵심 축이다.
