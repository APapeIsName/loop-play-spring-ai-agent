# Round 2 학습 기록

## 내가 배운 것

### 1단계 회고 (작성 시점에 작성됨)

Round 1에서 *프롬프트 자체를 다듬는* 훈련을 했다면, Round 2 1단계에서는 *프롬프트가 Tool 호출 의사결정에 어떻게 작용하는지* 를 50회 단위로 측정해보며 배웠다.

가장 큰 배움은 **"좋아 보이는 정책 한 줄이 정작 더 큰 회귀를 만든다"** 였다. v1의 약점(scn 2 underuse 5/10)을 잡으려고 다섯 번 System Prompt를 만졌는데, 다섯 번 다 회귀했다. 의도와 결과가 정반대로 가는 경험. 강의 2.4.1·2.4.2가 "System Prompt가 description의 효과를 가린다"고 경고한 내용을, 데이터로 직접 확인했다.

두 번째 배움은 **`@ToolParam` 예시가 hallucination fallback으로 베껴진다는 사실**. scn 4(\"취소해주세요\", reason 단서 없음)에서 9/9 trial이 `reason="집 앞에 사람이 없어요"` 라는 동일 가짜 사유로 호출됐고, 그 어구가 정확히 `@ToolParam(description = "... 예: '집 앞에 사람이 없어요'")` 의 예시였다. 데이터 품질 측면에서 무서운 패턴이다 — 호출 자체는 일어났으니 metric은 \"성공\"으로 잡히지만, 시스템에 가짜 사유가 누적된다. 이런 종류의 버그는 호출률만 보고는 절대 못 잡는다.

세 번째 배움은 **반복 측정의 가치**. 첫 단발 테스트에서는 5종 중 2종만 Tool이 호출되어 \"40%\"로 보였다(주문 4번이 중국어, 5번도 혼재). 그런데 같은 시나리오를 10회씩 돌리니 평균 84%로 안정화됐다. LLM 평가는 단발이면 noise에 묻히고, 반복이면 진짜 신호가 보인다. Round 1 피드백을 살린 부분.

네 번째 배움은 **판단(LLM)과 실행(Spring Bean)의 경계**가 실제로 어떻게 그어지는지. `@Tool` 어노테이션 하나로 메서드를 LLM에 노출하고, 결정·인자 채우기는 LLM에, 실제 호출·DB 변경은 Spring Bean에 맡기는 구조. Tool은 평범한 Spring Bean이라서 단위 테스트도 그대로 가능 — 이게 \"챗봇이 아니라 에이전트\"라는 표현의 실체였다.

### 2·3·4단계 통합 회고 (5가지)

Round 2 전체를 마치고 가장 크게 체감한 다섯 가지를 정리합니다.

#### 1. 반복 측정(테스트당 10회) + 지속적 개선의 가치

단순히 한 번 짜고 끝나는 게 아니라, 같은 시나리오를 10회씩 반복 측정하고 가설을 세워 변경하고 또 측정하는 사이클이 결정적이라는 걸 배웠습니다. 단발 측정으로는 noise에 묻혀 진짜 패턴이 안 보이고, 가설 검증 없이 무작정 개선만 하는 것도 위험합니다. 우리 1단계 v1~v5 250 trial 데이터가 정확히 이 사이클의 산출물이었고, 다섯 번 모두 회귀했다는 *부정적* 결과 자체가 가치 있는 학습이 되었습니다.

특히 첫 단발 테스트에서 5종 중 2종만 호출되어 \"40%\"로 보였던 것이 10회 반복 평균은 84%로 안정화된 경험은, *측정의 양*이 데이터 해석을 완전히 바꿔놓는다는 걸 실감하게 했습니다.

#### 2. enum 출력란이 LLM의 *확실한 답변 유도*에 결정적

Outcome enum(`CANCELED` / `ALREADY_CANCELED` / `NOT_CANCELABLE` / `NOT_FOUND`)처럼 출력란을 명시적으로 정해두고 LLM에 주는 것이, 추론 모델인 LLM이 작업 후 *확실한 답변*을 만드는 데 큰 도움이 됐습니다. `UNKNOWN`이나 `FAILED` 같은 모호한 값을 넣지 않은 이유도 같은 맥락 — LLM 입장에서 그건 HTTP 500 같은 서버 내부 장애로 인식되어 *추론에 의존*하게 되고, 그 추론은 hallucinate를 부른다는 걸 1단계 reason hallucination 사례에서 직접 봤습니다.

#### 3. 멱등성의 중요성 — 시스템 실재와 응답 표면의 분리를 막는다

`cancelOrder` 같이 부작용을 일으키는 Tool은 같은 요청이 여러 번 와도 *같은 응답을 재전달*하는 게 LLM 기반 시스템에서 정답이라는 걸 2단계에서 직접 데이터로 확인했습니다. Stage B에서 `ALREADY_CANCELED` + `NOT_CANCELABLE` 분기를 둘 다 제거했을 때 `canceledReason`이 두 번째 발화의 reason(*\"한 번 더 취소해주세요\"*)으로 덮어쓰이는 걸 보고, *멱등성 분기 하나의 부재*가 어떻게 *시스템 실재 ↔ LLM 응답 표면 분리*라는 가장 위험한 패턴을 만들어내는지 체감했습니다.

#### 4. description은 짧아질수록 호출률이 감소 — 4요소가 데이터로 증명

3단계에서 description을 4요소 풀버전(13줄) → A(강의 자료 4줄) → B(한 줄) → C(오해 유발 한 줄)로 줄여가며 호출률이 10/10 → 5/10 → 0/10 → 0/10으로 *단조 감소*하는 걸 본 게 가장 명확한 정량 데이터였습니다. 특히 *무엇/언제/입력/실패* 4가지 요소가 description에서 사라지거나 추가될 때 호출률이 정확히 달라지는 패턴은, *description = LLM에게 보여주는 유일한 API 문서*라는 강의 메시지를 데이터로 증명한 셈입니다.

#### 5. Tool이 Agent의 정확도와 토큰 효율 양쪽에 도움

Tool 자체는 Agent의 정확도와 토큰 효율 양쪽에 결정적입니다.

**정확도 측면**: 외부 시스템에서 가져와야 할 데이터를 LLM이 추론하는 대신 Tool로 위임하면 hallucinate 위험이 크게 줄어듭니다. 1단계 scn 1에서 *\"역삼역 사거리\"* 라는 *시스템에만 있는 정보*를 LLM이 정확히 응답에 담을 수 있던 것도 Tool이 그 데이터를 가져왔기 때문입니다.

**토큰 효율 측면**: Tool이 없으면 LLM이 같은 일을 하기 위해 *모든 주문·메뉴·라이더 정보를 컨텍스트에 미리 dump*해야 하는데, Tool은 *필요한 시점에 필요한 작은 DTO*만 가져오니 결과적으로 *데이터 범위를 좁히는* 효과를 냅니다. 우리 `OrderDetailView`도 의도적으로 내부 `Order` 엔티티의 4개 필드(`deliveryAddress`·`riderLocation`·`canceledReason`·`canceledAt`)를 제외해 *작게 자른* 결과물이고, 이 결정 하나만으로도 매 호출의 입력 토큰을 줄였습니다.

다만 4단계에서 본 것처럼, Tool *등록* 자체로 입력 토큰이 chat baseline 대비 +38배 늘어나는 *고정 비용*은 있습니다. 즉 *가상의 \"전체 dump 대비 절약\"*이지 *Tool 없는 단순 chat 대비 절약*은 아닙니다. *어떤 Tool을 등록할지·description을 얼마나 자세히 쓸지*가 운영 의사결정의 핵심이 됩니다.

#### 종합

Round 2는 *Tool Calling이라는 메커니즘*을 배우는 것보다 *Tool의 경계를 설계하고 그 경계의 영향을 측정하는 사고방식*을 익히는 시간이었습니다. enum·멱등성·description 4요소 모두 결국 *LLM이 추론에 의존하지 않게 시스템이 미리 정답을 제공하는* 방향의 디자인 결정들이었고, 측정 사이클 없이는 그 영향을 알 수 없었습니다.

## 의문점

### 1단계 회고

가장 큰 의문은 **\"왜 v5가 갑자기 Tool 호출 JSON을 텍스트로 뱉었나?\"** 다. v5에서 \"정보가 부족할 때\"를 \"주문번호와 맞는 정보가 없다고 판단했을 때\"로 좁힌 한 줄 변경이, 일부 trial에서 모델이 응답에 `{"name": "cancelOrder", "arguments": {...}}` 같은 JSON 호출 시그니처를 *텍스트로* 박아넣게 만들었다. 실제 Spring AI 호출은 일어나지 않은 채로. \"판단했을 때\"라는 메타 단어가 모델에게 \"내가 판단 과정을 보여줘야 한다\"는 신호로 작동한 것 같은데, 이 정도로 미세한 표현 차이가 호출 메커니즘 자체를 바꿔놓는다는 게 놀랍고 무섭다.

두 번째 의문 — **description 길이의 적정선**. 우리 description은 [언제 호출]/[언제 호출하지 않음]/[입력]/[실패 시] 4섹션으로 꽤 길다. 강의 2.3 예시도 비슷한 길이. 그런데 4부 \"입력 토큰이 왜 갑자기 늘었는가\" 절에서 \"description을 간결하게\"가 운영 최적화 권고. 정확도 vs 토큰 비용의 trade-off 곡선이 어떻게 생겼는지, 짧은 description으로도 v1 84%를 유지할 수 있는지 측정해보고 싶다.

세 번째 의문 — **`@ToolParam(reason)` 예시 제거(또는 sentinel 명시) 시도(v4)가 왜 scn 4를 0/10으로 만들었나?** 호출률을 떨어뜨리지 않고 reason hallucination만 잡는 방법이 있는지. \"reason은 선택적이고 없으면 빈 문자열\"을 어떻게 표현해야 모델이 호출 의지를 유지하는지.

### 2·3·4단계 의문점 (3가지)

#### 1. `_internal/reset` 호출이 왜 Tool 호출률을 떨어뜨렸을까?

가장 큰 미스터리입니다. 1단계 v1에서 1236 cancel 발화는 9/10 호출됐었는데, 2단계 outcome 측정에서 매 trial 직전 `/api/v1/_internal/reset`을 호출하기 시작하니 동일 1236 cancel이 1/10으로 떨어졌습니다. `ChatClient`는 stateless라 reset endpoint 호출이 직접 영향을 줄 이유가 없는데도 호출률이 일관되게 떨어진다는 게 신기합니다.

Ollama 측의 KV 캐시 상태가 reset 사이에 invalidate 되는 것인지, HTTP 연결 풀의 상태 변화인지, 아니면 *연속된 동일 발화 mix 안에서 LLM이 받는 신호*가 미세하게 다른 것인지 — 명확한 원인을 찾지 못했습니다. *측정 환경 자체가 결과에 영향을 주는* 케이스라 디버깅이 어려웠고, 이게 *측정의 재현성*에 대한 의문으로도 이어집니다.

#### 2. description 길이의 적정선은 어디인가?

3단계에서 description을 줄일수록 호출률이 단조 감소하는 걸 봤습니다. 하지만 *그 반대 방향*도 마찬가지로 궁금합니다 — 4요소 풀버전(13줄)과 강의 자료 A(4줄)가 입력 토큰은 거의 같은데 호출률은 100% vs 50%였습니다. 즉 *토큰 비용을 늘리지 않고도 호출률을 크게 끌어올릴 수 있다*는 점에서, 어디까지 더 늘려야 의미가 있는지 한계선을 정할 수 없었습니다.

핵심 질문: **description 길이가 길다고 해서 무조건 좋은 걸까? 토큰 상관없이 정확도가 길이와 비례해서 올라갈까?** 더 적은 토큰으로 동일 호출률을 내는 *압축된 description*은 가능한가? 길이 vs 호출률 곡선이 어디서 saturate 되는지 측정하는 후속 실험이 필요합니다.

#### 3. \"응답 표면 ↔ 시스템 실재 분리\" 패턴은 어떻게 모니터링 하나?

Stage B pair 1에서 *시스템은 새 reason으로 덮어쓰였는데 LLM은 \"이미 취소\"라고 답한* 사례가 가장 무서운 패턴이었습니다. 이런 *LLM 응답과 시스템 상태의 불일치*는 호출률 metric이나 응답 자연성 검증만으로는 잡히지 않습니다. 호출은 정상이고 응답도 그럴듯해 보이는데 *시스템 실재*는 다른 상태인 셈이라, *겉만 보는 모니터링*으론 안 통합니다.

이걸 자동으로 검증하는 방법이 가능할까? PR 시점에 *LLM 응답의 주장(claim)*과 *시스템 상태(state)*의 일치성을 검사하는 테스트가 필요할 텐데, 그게 어떤 형태일지 — Tool 호출 후 시스템 상태를 다시 한 번 read해서 LLM 응답에 포함된 주장과 비교하는 *assertive verification*? 그리고 그게 *모든 응답·시스템 상태 쌍*에 대해 scale 가능한가? — 아직 답을 못 찾은 상태입니다.

## Round 3에서 시도하고 싶은 것

### 1단계 회고 시점

Round 3가 Chat Memory와 conversationId 다회 턴이라면, 1단계의 약점이 자연스럽게 정리될 가능성이 있다. 즉:

- **\"그거 취소해주세요\" 같은 지시 대명사** — 1단계에선 매 발화가 독립이라 \"그거\"가 무엇인지 모름. Memory가 들어오면 직전 턴의 orderId가 컨텍스트에 살아 있어서, Tool 호출 시 자동으로 채워질 수 있을 듯. 그러면 1단계 v1에서 발견한 scn 2 모방 패턴(\"주문번호를 알려주시겠어요?\")도 줄 수 있겠다 — 모델이 직전 메모리에서 orderId를 가져올 수 있으니까.
- **conversationId 격리** — 1단계의 reason hallucination도 Memory 안에서 \"이번 대화에서 고객이 명시한 사유\"가 누적되면 더 자연스럽게 풀릴지 측정해보고 싶다.

또 1단계에서 못 푼 **scn 2 5/10 문제를 다른 lever로** 다시 잡아보고 싶다. System Prompt는 막혔으니 — Tool 등록 순서, `@Tool(name)` 명시, description 첫 줄 한 줄 트리거 요약 같은 다른 축으로. 그 측정도 50회 반복으로 신호 잡기.

마지막으로 **멱등성(2단계)** 자체. \"같은 cancelOrder를 두 번 부르면\"이라는 시나리오를 실제로 돌려보면서 ALREADY_CANCELED enum 값이 LLM에게 어떻게 전달되어 어떤 자연어로 변환되는지 — Outcome enum의 이름이 자연어처럼 읽혀야 LLM이 의미를 파악한다는 강의 메시지를 실측으로 확인하는 게 다음 핵심 학습 포인트.

### 2·3·4단계 누적 아이디어 — 현재 미해결 문제를 Memory로 풀 수 있을까

2·3·4단계를 거치면서 본 *현재 미해결 문제들*을 Memory가 어떻게 해결해 줄 수 있을지 가설을 세우고, Round 3에서 측정해보고 싶습니다.

#### 1. reason hallucination을 Memory로 잡을 수 있을까

1단계 scn 4에서 본 9/9 동일 hallucination — \"취소해주세요\" 만 있는 발화에 `reason=\"집 앞에 사람이 없어요\"`가 박힌 패턴 — 은 *현재 발화 안에 reason 단서가 없기 때문에 발생*한 것이었습니다. Memory가 도입되면 *직전 턴에서 사용자가 실제로 말한 사유*(예: \"라이더가 늦어서요\", \"메뉴를 잘못 시켰어요\")가 컨텍스트에 살아 있으므로 reason 자리를 *진짜 사용자 말*로 채울 수 있지 않을까.

단발 발화에서는 빈 자리를 `@ToolParam` 예시로 메워야 했지만, 다회 턴이면 *대화 흐름 안에서 자연스럽게* 채워질 가능성이 있습니다. 이 가설을 *1단계 scn 4 시나리오를 다회 턴 버전*으로 확장해 측정해보고 싶습니다 — 예: \"방금 시킨 거 좀 늦네요\" → \"그러면 취소해주세요\".

#### 2. Stage B의 \"응답 표면 ↔ 시스템 실재 분리\"를 Memory가 해결할 수 있을까

위 의문점 3에서 다룬 가장 무서운 패턴 — *시스템은 새 reason으로 덮어쓰였는데 LLM은 \"이미 취소\"라고 답함*. 이게 발생한 원인은 LLM이 *시스템 상태에 대한 직전 기억이 없어* 추론에 의존했기 때문이라고 봅니다.

Memory가 *직전 cancelOrder의 Outcome enum 결과*를 보관한다면, 두 번째 발화에서 LLM이 *시스템 실재와 일치하는* 응답을 만들 수 있을지 — 즉 **Memory가 시스템 상태의 source-of-truth 역할**로 작동하는지 확인하고 싶습니다. 이건 의문점 3을 *직접 해결하려는 시도*라는 점에서 학습 흐름이 자연스럽게 이어집니다.

---

이 둘이 풀리는지 Round 3에서 확인하는 것 — *현재 문제의 직접 해결*이라는 점에서 학습 동기가 가장 명확합니다.
